
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.atmanamviddhi.in',
  appName: 'Atmanam Viddhi - Know Thyself',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
